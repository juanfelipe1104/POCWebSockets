# passkey-challenge-lambda

AWS Lambda (Java 17) + Spring Cloud Function + arquitectura hexagonal.
Función: `createChallenge`.

## Build & test
```bash
mvn -q test
```
JaCoCo genera reporte en `target/site/jacoco` y valida cobertura mínima 90%.
